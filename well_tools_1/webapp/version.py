"""Single source of truth for the application version.

Bumped per release; a git tag ``v<__version__>`` triggers the release build.
Stamped into the built exe and surfaced in the UI (About/Help).
"""

__version__ = "0.1.0"
