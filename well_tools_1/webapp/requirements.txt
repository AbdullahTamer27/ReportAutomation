# Web app dependencies (Phase 1+). Install with:
#   pip install -r webapp/requirements.txt

# --- Web framework / server (Phase 1) ---
fastapi
uvicorn[standard]

# --- Report history DB (Phase 3) ---
sqlalchemy

# --- Native window + packaging support (Phase 5) ---
pywebview

# --- PDF rendering + preview (Phase 4) ---
pymupdf
docx2pdf

# --- Report engine dependencies (already used by well_tools) ---
python-docx
openpyxl
pandas
lxml
# matplotlib + numpy are PINNED: the CI-built exe must render the pie charts
# identically to the dev machine. An unpinned matplotlib pulled a version whose
# pie-label behaviour differed and rendered the chart numbers blank in the exe.
matplotlib==3.11.0
numpy==2.4.6