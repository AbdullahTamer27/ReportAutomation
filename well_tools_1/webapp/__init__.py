"""Web app package for the report-automation tool (FastAPI).

Lives alongside the existing `well_tools` package and calls its report engine
(`well_tools.report.report_builder.build_automation_report`) without modifying it.
"""
